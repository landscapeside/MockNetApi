<resources>

    <!-- Base application theme. -->
    <style name="MockApiAppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mockColorPrimary</item>
        <item name="colorPrimaryDark">@color/mockColorPrimaryDark</item>
        <item name="colorAccent">@color/mockColorAccent</item>
    </style>

</resources>
